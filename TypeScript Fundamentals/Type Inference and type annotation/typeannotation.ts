// Type Annotation means we explicitly tell TypeScript

// Explicitly annotated as number
let age: number = 25

// Explicitly annotated as string
let name: string = "Raj"

// Explicitly annotated as boolean
let isActive: boolean = true

console.log(age, name, isActive)
